import React, { Component } from 'react';
import { CPUType } from '../../emulator/cpu';

interface ROMPanelProps {
    cpu: CPUType
}

class CPUPanel extends Component<ROMPanelProps, any> {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default CPUPanel;
