

type RGBColor = {
    red: number,
    green: number,
    blue: number
}


type DisplayMode = 'canvas' | 'image';

class Display {

    setPixel(x: number, y: number, color: RGBColor) {

    }

}

export { Display };
export type { RGBColor };